---
version: 0.2
phases: 
  install:
    runtime-versions:
      nodejs: 24
    commands: 
      - echo "insalling tools"
      - npm --v
      - node -v
      - npm install
  build:
    commands:
      - npm run build

artifacts: 
  directory: ./dist/frontend/browser